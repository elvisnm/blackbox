#!/bin/sh
SCRIPT_PATH="$(readlink -f "$0" 2>/dev/null || perl -MCwd -e 'print Cwd::abs_path shift' "$0")"
SCRIPT_DIR="$(dirname "$SCRIPT_PATH")"
exec "$SCRIPT_DIR/../node_modules/.bin/tsx" "$SCRIPT_DIR/bbox.ts" "$@"
